---
"drift-toolkit": minor
---

Add automatic tracking of GitHub Actions workflow files (.github/workflows/*.yml and .github/workflows/*.yaml) as always-tracked files, independent of cm dependencies output.
