---
"drift-toolkit": minor
---

Add dependency file change detection that tracks all configuration files from cm dependencies, not just check.toml. Includes grouping by check type and glob pattern support.
