---
"drift-toolkit": patch
---

Add process scan to drift-scan.yml workflow. Users can now run code scans, process scans, or both via the scan_type input. Scheduled runs execute both scans by default.
